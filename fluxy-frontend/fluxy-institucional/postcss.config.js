// Tailwind v4 usa @tailwindcss/vite — nenhum plugin PostCSS necessário.
// Este arquivo existe apenas para impedir o Vite/PostCSS de herdar o
// postcss.config.js do diretório pai (fluxy-frontend/), que carrega Tailwind v3.
export default {
  plugins: {},
}
