services:
  iefc-db:
    image: postgres
    container_name: iefc_container
    environment:
      POSTGRES_PASSWORD: ${IEFC_DATABASE_PASSWORD}
      POSTGRES_USER: ${IEFC_DATABASE_USERNAME}
      POSTGRES_DB: ${IEFC_DATABASE_NAME}
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      - iefc-db-data:/var/lib/postgresql/data
    networks:
      - iefc-network
  fluxy-db:
    image: postgres
    container_name: fluxy_container
    environment:
      POSTGRES_PASSWORD: ${FLUXY_DATABASE_PASSWORD}
      POSTGRES_USER: ${FLUXY_DATABASE_USERNAME}
      POSTGRES_DB: ${FLUXY_DATABASE_NAME}
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
     - "5433:5432"
    volumes:
      - fluxy-db-data:/var/lib/postgresql/data
    networks:
      - fluxy-network
  iefc-api:
    container_name: iefc_backend
    build:
      context: ./iefc-backend
      dockerfile: Dockerfile
    ports:
      - ${IEFC_APP_PORT}:${IEFC_APP_PORT}
    volumes:
      - ./iefc-backend:/app
    env_file:
      - .env
    depends_on:
      - iefc-db
    networks:
      - iefc-network
  fluxy-api:
    container_name: fluxy-backend
    build:
      context: ./fluxy-backend
      dockerfile: Dockerfile
    ports:
      - ${FLUXY_APP_PORT}:${FLUXY_APP_PORT}
    volumes:
      - ./fluxy-backend:/app
    env_file:
      - .env
    depends_on:
      - fluxy-db
    networks:
      - fluxy-network
volumes:
  iefc-db-data:
  fluxy-db-data:

networks:
  fluxy-network:
    driver: bridge
  iefc-network:
    driver: bridge
